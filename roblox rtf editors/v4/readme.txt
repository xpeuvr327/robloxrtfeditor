issues:only 1 style working at the time. don't forget to close arguments in the reverse side you opened them.
new features: make each cursor actually being the color (ex: make the red cursor red, etc.), make a title (roblox rtf editor) in a cool font, make the the words gras, italique souligné, barré another font than the default (de préférence de la même famille que le titre), mettre la taille de la boite de sortie la même taille que la largeur de la fenêtre, inclure un petit fichier readme.txt qui donne un bref résumé des fonctionnalités et des derniers changements et inclure le numéro de version (v0.5 pour la version suivante)